// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://deadhood97.github.io',
  base: '/matterofmatter',
  build: {
    format: 'file',
  },
});
